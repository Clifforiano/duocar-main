export interface Reserva {
    id_pasajero: string; // ID del pasajero que hace la reserva
    id_viaje: string;    // ID del viaje al que se hace la reserva
    asiento: number;     // Número de asiento reservadp
}